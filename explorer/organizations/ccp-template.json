{
    "name": "test-network-org${ORG}",
    "version": "1.0.0",
    "client": {
        "organization": "Org${ORG}",
        "connection": {
            "timeout": {
                "peer": {
                    "endorser": "300"
                }
            }
        }
    },
    "organizations": {
        "Org${ORG}": {
            "mspid": "${NAME}",
            "peers": [
                "peer0.${NAME}"
            ],
            "certificateAuthorities": [
                "ca.org${ORG}"
            ]
        }
    },
    "peers": {
        "peer0.${NAME}": {
            "url": "grpcs://localhost:${P0PORT}",
            "tlsCACerts": {
                "pem": "${PEERPEM}"
            },
            "grpcOptions": {
                "ssl-target-name-override": "peer0.${NAME}",
                "hostnameOverride": "peer0.${NAME}"
            }
        }
    },
    "certificateAuthorities": {
        "ca.org${ORG}": {
            "url": "https://localhost:${CAPORT}",
            "caName": "ca-org${ORG}",
            "tlsCACerts": {
                "pem": ["${CAPEM}"]
            },
            "httpOptions": {
                "verify": false
            }
        }
    }
}